// Mempool decoder binary — standalone WebSocket listener + ABI decoder + Redis publisher.
// Implementation deferred to Phase 10.

fn main() {
    println!("mempool-decoder: not yet implemented (Phase 10)");
}
